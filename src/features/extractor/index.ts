/* extractor — public entry point. Import from '@/features/extractor' rather than
   reaching into individual files, so internals can move freely. */
export { DataExtractorPage } from './DataExtractorPage';
